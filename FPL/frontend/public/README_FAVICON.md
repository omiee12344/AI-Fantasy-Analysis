# Favicon

The app uses a simple inline SVG favicon (text "AI FA") defined in `index.html`. No external logo asset is required.

To use a custom favicon, add `favicon.ico` or `favicon.png` to this directory and update the `<link rel="icon">` in `frontend/index.html`.
